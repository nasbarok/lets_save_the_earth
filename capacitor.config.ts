import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.letsavetheearth.game',
  appName: "Let's Save the Earth",
  webDir: 'dist'  // Changé de 'www' vers 'dist'
};

export default config;